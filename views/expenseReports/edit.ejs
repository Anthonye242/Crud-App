<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Expense Report - Personal Finance Tracker</title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body>
  <%- include('../partials/nav') %>

  <div class="container">
    <h1>Edit Expense Report</h1>

    <form action="/expenseReports/<%= report._id %>?_method=PUT" method="POST">
      <label for="title">Title:</label>
      <input type="text" id="title" name="title" value="<%= report.title %>" required>

      <label for="startDate">Start Date:</label>
      <input type="date" id="startDate" name="startDate" value="<%= report.startDate.toISOString().split('T')[0] %>" required>

      <label for="endDate">End Date:</label>
      <input type="date" id="endDate" name="endDate" value="<%= report.endDate.toISOString().split('T')[0] %>" required>

      <button type="submit" class="button">Update Expense Report</button>
    </form>
  </div>
</body>
</html>
